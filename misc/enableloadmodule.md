# EnableLoadModule

search for string "EnableLoadModule", second xref:

```c
__int64 sub_4265E80()
{
  return sub_493FF40(a1: qword_887CA40, a2: "EnableLoadModule", a3: &byte_8496288, a4: 1);
}
```

byte_8496288 is the offset, so the offset is 0x8496288
